# Model files — too large for git (use HuggingFace Hub instead)
models/*.gguf
*.gguf
*.guf
**/*.gguf
**/*.guf
models/*.bin
models/*.safetensors

# HuggingFace download cache
models/.cache/

# Build output
llama.cpp/build/

# Audio samples (regenerate with gTTS)
samples/

# macOS
.DS_Store
.history/