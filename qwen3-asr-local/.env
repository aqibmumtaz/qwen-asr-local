# Default runtime config for the Roman-Urdu pipeline (hindi_to_roman_urdu.py).
# Real environment variables OVERRIDE these (setdefault), so the env-var mechanism wins.
LEXICON=v22
PHONETIC=1
PHONETIC_THRESHOLD=0.9
RESOLVER=0
